{{- if .Values.ingress.enabled -}}
{{- $root := . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "borjie.fullname" . }}
  labels:
    {{- include "borjie.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - hosts:
        - api.borjie.com
        {{- range $name, $app := .Values.apps }}
        {{- if $app.enabled }}
        - {{ $app.host }}
        {{- end }}
        {{- end }}
      secretName: {{ .Values.ingress.tlsSecretName }}
  rules:
    - host: api.borjie.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "borjie.fullname" . }}-gateway
                port:
                  name: http
    {{- range $name, $app := .Values.apps }}
    {{- if $app.enabled }}
    - host: {{ $app.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "borjie.fullname" $root }}-{{ $name | kebabcase }}
                port:
                  name: http
    {{- end }}
    {{- end }}
{{- end -}}
