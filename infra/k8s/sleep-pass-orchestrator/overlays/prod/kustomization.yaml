apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: borjie-prod

resources:
  - ../../base

images:
  - name: ghcr.io/georgemwiki/borjie-sleep-pass-orchestrator
    newTag: ${IMAGE_TAG}

patches:
  - target:
      kind: Deployment
      name: sleep-pass-orchestrator
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
      - op: replace
        path: /spec/template/spec/containers/0/resources
        value:
          requests:
            cpu: "300m"
            memory: "384Mi"
          limits:
            cpu: "1500m"
            memory: "1.5Gi"
      - op: replace
        path: /spec/template/spec/affinity/podAntiAffinity
        value:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/name: sleep-pass-orchestrator
              topologyKey: kubernetes.io/hostname
