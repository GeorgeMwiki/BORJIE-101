/**
 * EnvBadge primitive — LitFin-parity re-export.
 *
 * Public path for the env pill used by the admin TopBar. The actual
 * implementation lives at `components/admin-shell/EnvBadge.tsx`.
 */

export { EnvBadge } from '@/components/admin-shell/EnvBadge';
