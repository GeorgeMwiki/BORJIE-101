{
  "name": "@borjie/ledger-attestor",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Ledger attestor (LP-19). Computes a Merkle root over the audit/ledger hash-chain, signs the root via an injected SignerPort, and publishes the signed checkpoint to a pluggable ExternalSinkPort (S3 object-lock / transparency-log). Tamper-evidence control for the royalty/treasury ledger. Builds on @borjie/audit-hash-chain.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@borjie/audit-hash-chain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "@types/node": "^25.9.3"
  }
}
