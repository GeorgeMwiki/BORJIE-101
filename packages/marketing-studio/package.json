{
  "name": "@borjie/marketing-studio",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Marketing & Promotion Layer 1-2-3-4 — the 12-channel campaign orchestrator, audience-segmented composers, multi-channel adapters, A/B testing with Bayesian decider, claims-citation compliance, SEO + JSON-LD, telemetry + UTM tagging, audit-chain linking. Implements docs/DESIGN/MARKETING_PROMOTION_SPEC.md.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist coverage",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@borjie/audit-hash-chain": "workspace:*",
    "undici": "^6.26.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
