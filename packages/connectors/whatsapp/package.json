{
  "name": "@borjie/connector-whatsapp",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "OMNI-P0-BATCH-2 — WhatsApp Business Cloud API ingest connector. Inbound via webhook (HMAC-SHA256 X-Hub-Signature-256 verify), reconciliation poll on a 6h cadence, salted-hash PII redaction, encrypted-at-rest tokens (bytea). Persona: Mr. Mwikila.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
