{
  "name": "@borjie/connector-salesforce",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Salesforce connector for Wave OMNI-P1. Ingests accounts, opportunities, contacts, cases via SOQL polling + salted-hash PII redaction + audit-hash provenance. Implements the OmnidataConnector contract from @borjie/omnidata.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
