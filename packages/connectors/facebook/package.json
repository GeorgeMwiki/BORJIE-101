{
  "name": "@borjie/connector-facebook",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "OMNI-P2 — Facebook Page Graph API ingest connector. Shared Facebook OAuth2 broker (with Instagram), Page webhooks for feed events + 6h reconciliation poll. Salted-hash PII redaction. Persona: Mr. Mwikila.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
