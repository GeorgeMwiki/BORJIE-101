{
  "name": "@borjie/connector-jira",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Jira connector for Wave OMNI-P1. Ingests issues, epics, sprints, worklogs via Atlassian OAuth 2.0 (3LO) + JQL polling + webhook signature verification, salted-hash PII redaction, audit-hash provenance.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": { "@types/node": "^25.8.0", "typescript": "^6.0.3", "vitest": "^4.1.6" }
}
