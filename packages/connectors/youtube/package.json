{
  "name": "@borjie/connector-youtube",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "OMNI-P2 — YouTube Data API v3 ingest connector. Google OAuth2 (youtube.readonly, yt-analytics.readonly). PuSH webhook receiver + 6h poll for statistics refresh. Salted-hash PII redaction for commenter channel IDs. Persona: Mr. Mwikila.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
