{
  "name": "@borjie/regulator-sim",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Regulator-readiness simulation (LP-16 / PO-20). Audit-replay over a date range asserting decision invariants (CoT present, bilingual notes, model-card freshness, four-eye distinct approvers, fairness deltas) + Tanzania PDPA subject-access/erasure drills + a mining-regulator supervision document pack. Pure functions, no I/O. Ported from LITFIN security/regulator-sim, re-skinned to mining licence/royalty/payout.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
