/**
 * Universal Cmd-K command palette - Wave SUPERPOWERS.
 *
 * Mounted in all three web apps (admin / owner / marketing) via the
 * root layout. The host app supplies the items catalog (Navigate /
 * Actions / Recent / Spawn tab / Settings).
 */

export {
  CommandPalette,
  type CommandPaletteProps,
  type CommandItem,
  type CommandKind,
} from './CommandPalette';
