/**
 * @borjie/emotional-prosody — Public API
 *
 * Types ported verbatim from sibling-port src/core/emotional-prosody/types.ts.
 * The full runtime (frustration-detector, prosody-prompt-context,
 * voice-warmth-controller) lives in the sibling source tree under sibling
 * files; this package supplies the shared contract so Borjie's persona-voice
 * runtime can interoperate.
 */

export * from "./types";
