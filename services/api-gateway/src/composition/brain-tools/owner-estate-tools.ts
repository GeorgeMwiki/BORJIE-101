/**
 * Owner estate persona — estate domain tools.
 *
 * Covers: subsidiaries, holdings, family office, succession planning,
 * ancillary, and asset register slices accessible to T1 strategists.
 */

import type { PersonaToolDescriptor } from './types';

const OWNER_ESTATE: ReadonlyArray<'T1_owner_strategist'> = ['T1_owner_strategist'];

export const OWNER_ESTATE_TOOLS: ReadonlyArray<PersonaToolDescriptor<any, any>> = Object.freeze([
  // Estate domain tools placeholder — wired but empty pending feature completion
]);
